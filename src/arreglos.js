/**
 * Lista edades de usuario
 * @type { Array }
 */

const edades = [4, 18, 55, 34, {}, true];

/**
 * Lista de arreglos de puntajes
 * @type { Array<Number> }
 */
const puntajes = [1, 5, 2.5, 9.7, 0.4];

/**
 * Lista de arreglos de puntajes
 * @type { Array<Number | String> }
 */
const numeroStrings = [1, "5", "2.5", 9.7, "pepe"];